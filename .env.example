# Root-level reference for local docker-compose Postgres.
# Each app also has its own .env.example (apps/api, apps/web) — copy those to .env there.
POSTGRES_USER=dataroom
POSTGRES_PASSWORD=dataroom
POSTGRES_DB=dataroom
